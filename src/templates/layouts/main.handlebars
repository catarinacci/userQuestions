<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <style>
        .payment-info {
            margin: 20px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .data-section {
            margin: 10px 0;
        }
        pre {
            background-color: #f5f5f5;
            padding: 10px;
            border-radius: 4px;
            overflow-x: auto;
        }
    </style>
</head>
<body>
    {{{body}}}
</body>
</html>